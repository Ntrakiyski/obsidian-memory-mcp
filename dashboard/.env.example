# MCP Server URL (server-side only, used by API route /api/mcp)
# This is used by the Next.js API route to proxy requests to the MCP server
# For local development: http://localhost:6666/mcp
# For production: https://obsidian-mcp.trakiyski.work/mcp
MCP_URL=https://obsidian-mcp.trakiyski.work/mcp
